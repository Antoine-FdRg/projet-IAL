services:
  box-broker-client:
    image: ial/box-broker-client
    command: ["node", "dist/main.js"]
    depends_on:
      - setup-broker
    env_file: ".env.docker"
    networks:
      - nats-network
networks:
  nats-network:
    driver: bridge
